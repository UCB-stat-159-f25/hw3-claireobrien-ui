from ligotools import readligo as rl
import numpy as np

# could not get to this because I was struggling with the death of one of my extended family members, but I would have written two unit tests for this example. One of the unit tests would have checked if read_hdf5 was reading correctly, and I would have done this by supplying toy data. The other would have been a test for the loaddata function to ensure that the outputs were not changing when the data was loaded. 